#include <string>
#include "list.h"
#include "console.h"
#include <locale>
#include <locale.h>

int main(){
    // std::locale::global(std::locale(""));    
    // std::locale::global(std::locale(""));
    // setlocale(LC_ALL, "en_US.UTF-8");
    strart_session();
}